import { ContactUpdatePrototypeView } from "@/prototypes/contactUpdate/ContactUpdatePrototypeView"

/** 獨立沙盒：聯絡資料自助更新 UI 預覽，不接 DB、不連正式學生／intake 頁 */
export default function PrototypeContactUpdate() {
  return <ContactUpdatePrototypeView />
}
