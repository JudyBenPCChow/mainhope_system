import { TrialRecordsPrototypeView } from "@/prototypes/trialRecords/TrialRecordsPrototypeView"

/** 獨立沙盒：試堂紀錄 UI 預覽，不接 DB、不連正式 /TrialSessions */
export default function PrototypeTrialRecords() {
  return <TrialRecordsPrototypeView />
}
