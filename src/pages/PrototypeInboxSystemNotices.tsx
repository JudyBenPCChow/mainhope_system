import { InboxSystemNoticesPrototypeView } from "@/prototypes/inboxSystemNotices/InboxSystemNoticesPrototypeView"

/** 獨立沙盒：收件匣營運／系統分頁 UI 預覽，不接 DB、不連正式 /Inbox */
export default function PrototypeInboxSystemNotices() {
 return <InboxSystemNoticesPrototypeView />
}
