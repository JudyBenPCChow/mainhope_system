---
description: 新增 migration 後必須主動單檔套用到 linked 遠端；禁止全量 db push
globs: supabase/migrations/**/*.sql
alwaysApply: false
---

# Supabase migrations

- **只新增、不改既有** migration 檔。
- 本任務需要的 schema 變更寫完後，**主動**套用（勿等使用者提醒）：
  1. 優先 `npm run db:apply -- <本檔路徑>`
  2. 失敗／超時則立刻：`supabase db query --linked -f <本檔>` → `supabase migration repair --status applied <版本> --linked`
- 遠端／本地歷史不一致時**禁止** `supabase db push`；見 `docs/meta/SUPABASE_MIGRATION_APPLY.md` 與 skill `apply-supabase-migration`。
- 一次只套本任務相關檔，勿順便推其他 pending 本地 migration。
- 勿用 `migration list --output-format json`（常 timeout）；plain `migration list --linked` 即可。
