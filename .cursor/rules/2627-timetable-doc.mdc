---
description: >-
  2627 時間表：PATCH（只改段落、唔 regenerate）或 FULL（script 出新版 md／Word PDF）。
  用戶講 patch only、只改、差異段落、唔 regenerate、微調某班時段 → PATCH；
  講 bump 版、generate、docx、pdf、重排方案 → FULL。
globs: scripts/generate_2627_timetable_doc.py,docs/year/2627/timetable/**,docs/product/topics/2627-september-timetable.md
alwaysApply: false
---

# 2627 時間表

## 模式（先判斷，唔好自動升 FULL）

| 模式 | 觸發 | 做咩 |
| --- | --- | --- |
| **PATCH** | 改 1–N 行／刪加個別班；或講 patch only／只改／唔 regenerate | 只改列明檔案＋段落 |
| **SYNC** | 用戶 confirm：改動要對齊多份附件 | 逐檔 patch；仍唔跑 script 全量生成 |
| **FULL** | 明講 bump 版、generate、docx、pdf、改 `CLASSES`、重排成個方案 | 跟下方 FULL 流程 |

缺檔案／before→after 就問。patch 後附件可能唔一致 → 列「建議 SYNC 邊幾份」，等 confirm。

## PATCH 模式

**禁止（除非用戶逐項要求）：** 跑 `generate_2627_timetable_doc.py`（含 `--word`）；bump 版／新 `versions/` folder；重出四套 md；改 `CLASSES`／`CONFIRMED_TEACHER_CLASS_TIMES`；重排班號字母；順改 SCHEDULING_RULES／ops-guide／backlog。

**流程：** 只讀目標 md（＋必要 csv）→ 逐條 before→after → show diff → confirm 後 commit → 若要 docx/pdf 只對**已 patch 嘅現版 md** 跑 `--word`（唔 regenerate md）。

**慳 token：** 唔讀舊版本 folder；SYNC 前先列受影響檔，唔 parallel 讀四套。

**班號：** 取消班唔自動由 A 重排；空字母保留。

**回覆：** Patch 摘要｜改動｜Diff｜未改附件｜下一步（commit／SYNC／docx pdf）。

開局模板：`.cursor/rules/2627-timetable-patch-prompt.md`

## FULL 模式（出新方案版本）

- **先改 `CLASSES`／原則，再 bump 版號。** 4.x 自動 +0.1（4.0→4.1）；`PREV_VERSION`＝上一版；舊檔保留，唔好覆寫 2.0–3.x 或上一版 4.x。
- **預設只出 md**（方案、老師附件、獨立周時間表三份審閱稿）。用戶未講「出 pdf／docx／Word」就唔好跑 `--word`。
- `CONFIRMED_TEACHER_CLASS_TIMES` 係已確認老師班別時間鎖；未經用戶明確指示，不得改班別、逢星期或時段，亦不得更新該常數繞過驗證。
- 方案正文**不放周時間表／第 8 節**；周時間表必須用 `2627_timetable_weekly_v<版本>` 獨立生成。
- 出檔：`python3 scripts/generate_2627_timetable_doc.py` → `docs/year/2627/timetable/versions/v<版本>/` 內三份 md；加 `--word` → 同一版本 folder 的三份 docx＋由 **Microsoft Word 另存 PDF**（內建 TOC、頁首 STYLEREF／頁尾 PAGE、普通邊界 2.54cm）。禁止 reportlab 直出 PDF。
- 唔寫 production；ops-guide 唔列課表／老師名。改政策／ops-guide 正文 → `.cursor/skills/ops-policy-2627-sync/SKILL.md`（patch 時間表唔自動觸發）。
