# Session HANDOFF：非常規排程產品拍板（同一資料流）

| 欄位 | 值 |
| --- | --- |
| 日期 | 2026-08-31 |
| 主題／backlog | `docs/product/topics/nonstandard-scheduling.md` |
| 分支／工作樹 | `cursor/nonstandard-scheduling-product-5a90`；PR [#68](https://github.com/JudyBenPCChow/mainhope_system/pull/68) |
| 驗證 | 未驗證（本會話只改分題／交接，無程式、無 migration） |

## 目標
- 非常規排程（加堂／混合補堂）產品拍板；未拍板不實作。
- 收斂 3.1／3.2／3.3 產品句，並寫入分題。

## 已完成
- 覆核現況仍適用；分題改書面語（先前已合入 `main`：`4863f39a`／PR #64）。
- 本分支記錄拍板：見分題開工閘表與「同一資料流」。
- 產品句摘要（真源在分題，此處不重抄規格）：
  - **同一資料流：** 試堂、單堂報讀、3.2 混合補堂的**非原班生**＝一條「非本班就讀生綁該堂、不留宿主班就讀中」；差別只在標籤與扣哪一袋。
  - **3.1：** 界面標單堂報讀（不得標試堂）；出單確認後上紙；該堂後不留就讀中；已繳堂數入**該年級**專科池（科目共用、年級仍分開；不入試堂袋）。
  - **3.2 A：** 不做無班時段。原班生靠 3.3；非原班生走上面那條資料流。
  - **3.3：** 加堂勾選已就讀生；只有被選者上紙。不屬「非就讀生入場」。
- 試堂現行已是「並非入讀該班」。用語表單堂亦寫「並非入讀該班」；現行單堂報讀卻會建成就讀中——這是工程缺口。

## 未完成／卡住
- **未開工實作。** 產品未明示開工；開工閘「實作」波次尚未寫步驟。
- 授課人數：建議單堂預設計入；產品未反對亦未明示。
- 實作時須把現行三條入場（報讀宣告／試堂／調堂）收成一條「非就讀生綁該堂」管線——設計未寫。
- 工作樹其他題（功輔計糧／三線收費）與本題無關；本分支不要夾帶。

## 下一步（給新會話）
1. 讀 `docs/product/topics/nonstandard-scheduling.md` 開工閘與「同一資料流」。未獲「開工實作」明示：**停**，只可補產品句或答問。
2. 若用戶說開工：在**本分題**寫實作步驟（不另開 `plans/`）。先畫一條共用入場（非就讀生 × 該 `schedule_id`），再分叉標籤／扣袋；3.3 另做加堂挑選。不可重開手動加名，不可建無班時段，不可改 `BACKLOG.md` 索引表。
3. 實作後門檻：`npm run lint`／`npm run build`；改 schema 則單檔 migration＋RLS。

## 開局必讀（精簡）
- `AGENTS.md`（對使用者書面語；`makeup_of=` 勿刪）
- `docs/product/topics/nonstandard-scheduling.md`
- `src/lib/entitlementNamespace.ts`（專科池＝同一級共用；試堂獨立）

## 勿再踩
- 3.2 例子：**沒有**星期五中一 a 班 8 人 vs 星期六 b 班。8 人是 Katie **逢六**那一班；星期五 14:00 是擬開加堂；陳小明／王曉彤是那 8 人裡的 2 人。
- 不要建議單堂「不入專科池以免罰款」——與同級專科共用池相反。試堂才獨立。
- 不要把 3.1／3.2 非原班生／試堂做成三套 schema。3.3 不要塞進這條（已就讀）。
- 專科池不是跨年級一袋（中一 ≠ 中二）。不要重開 P6 手動加名。不要 commit `BACKLOG.md` 索引、`dist/`、`docs/generated/**`。

## 明確唔做
- 未明示開工：不改 schema、宣告、點名入場、試堂頁、調堂 UI。
- 不把約房「其他」改成補堂容器。
- 功課輔導班不補堂，不混入本題。
- 不重開 `makeup_of` 日落、`26SM` 遷池。
- 不寫給行政同事的完工摘要（那是 `mainhope-release-handoff`）。
