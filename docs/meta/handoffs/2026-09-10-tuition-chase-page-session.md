# Session HANDOFF：學費追收頁（期數單位）

| 欄位 | 值 |
| --- | --- |
| 日期 | 2026-09-10 |
| 主題／backlog | `docs/product/topics/tuition-chase-list.md`（`in_progress`；步 1–3c done，步 4 對庫＋UI 對照規格已收，未在瀏覽器親手點） |
| 分支／工作樹 | `feature/tuition-chase-page`；**全部未 commit** |

## 目標
- 行政側欄「學費追收」：按學生×已繳堂數組別列出本期／下期要追堂數；點列開共用右側預覽欄。
- 時間單位為**常規第 N 期**（附件甲），不是曆月。

## 已完成
- 步 1–3c（假期窗、缺池、一次過可收降級、密度／科目小計／宏觀、已收款過濾、行級建議）。
- 步 4 production 抽樣（2026-09-10）：宣告 vs 班全部排程 53 人中 10 人有差；已收查詢。
- 專科計堂改跟 active 宣告（單堂／中途／調堂）；退讀殘留 `enrollment_auto` 不計。
- 私人課程無學年、無池：預設名單仍列出，標「未有結餘資料」；不再標「已預繳」並隱藏。
- 同級單價抽樣：初中 $275、高中 $300，無同級異價。混級英文用 `class:<uuid>` 池（正確）。
- UI 對照規格（2026-09-10）：名單隊列／宏觀／缺池區、右側密度與一次過可收降級、學生分頁同組件；尚餘負數淡紅底。抽樣曾穎單堂英文＋私人缺池、文覺熲／劉展調堂目標堂入已扣。
- 清單 TTL 快取（2026-09-10 續做）：`tuitionChaseListState`；離開學生詳情／收款再返回不整表重抓；確認收款／出單／作廢會 invalidate。

## 未完成／卡住
- **未在瀏覽器親手點**（用戶預設不用 Browser MCP）。
- **未 commit／未開 PR**（用戶未要求）。
- 私人課程尚未鑄 2627 池（資料現況，不是本頁能鑄）。
- 調堂原堂若未點事假，仍在還會扣（點名資料；不改本頁接請假表）。
- 發佈配套（用語表／前線指引）未做。

## 下一步（給新會話）
1. 用戶要求時再 commit／開 PR（勿擅自推）。若要親手驗：搜尋曾穎、文覺熲、劉展；看缺池區與尚餘負數淡紅；開完整詳情再返回應即見名單。
2. 合入 `main` 後跑 `git-branch-closeout`；`BACKLOG.md` 索引只在 main 搬列。

## 開局必讀（精簡）
- `AGENTS.md`
- `docs/product/topics/tuition-chase-list.md`（§0、§4、§7）
- `src/services/tuitionChaseQueries.ts`、`src/lib/tuitionChaseDetail.ts`

## 勿再踩
- 追收單位**不要**退回曆月；必須用日期集合分桶。
- 專科不要用該班全部排程計還會扣（單堂會假欠）。
- 缺池不要當尚餘 0，也不要標「已預繳」藏進預設隱藏區。
- 不要改終身 `student_tuition_arrears`；不要把暑期堂／未繳上紙當缺陷。
- Feature 分支**不要改** `docs/product/BACKLOG.md` 索引表。

## 明確唔做
- 不要取代 AI「追收學費摘要」；不要自動出單／WhatsApp。
- 不要把收款引擎全面改成期數；不開「過往欠堂」術語；尚餘負不再加隊列首級。
- 未要求不要 commit／PR；不要 commit `dist/`。
